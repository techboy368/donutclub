/**
 * Main entry point – initialises all modules after DOM is ready.
 */
document.addEventListener('DOMContentLoaded', () => {
  // These modules auto‑initialize if their required elements exist.
  // Just importing them (via script tags) is enough.

  // You can add any global initialisation here.
  console.log('🍩 Donut Club ready!');
});
