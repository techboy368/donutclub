/**
 * Discord widget placeholder.
 * You can uncomment the code below to embed the Discord server widget if you have a widget URL.
 * For now, it simply logs a message.
 */
(function() {
  const widgetContainer = document.getElementById('discord-widget');
  if (!widgetContainer) return;

  // Uncomment and replace with your Discord server widget URL to enable.
  /*
  const iframe = document.createElement('iframe');
  iframe.src = 'https://discord.com/widget?id=YOUR_SERVER_ID&theme=dark';
  iframe.width = '350';
  iframe.height = '500';
  iframe.allowTransparency = 'true';
  iframe.frameBorder = '0';
  iframe.sandbox = 'allow-popups allow-popups-to-escape-sandbox allow-same-origin allow-scripts';
  widgetContainer.appendChild(iframe);
  */

  console.log('🍩 Discord widget ready (currently placeholder).');
})();
